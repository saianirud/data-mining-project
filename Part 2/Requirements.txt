Steps to Execute the code:
1. Run the file train.py
2. It will produce the output model.pkl in the same directory
3. Then run the file test.py
4. It will produce the output Results.csv in the same directory

Note:
1. The system uses Python 2.7
2. The input files should be in the same directory as the python files

Library versions:
backports.functools-lru-cache==1.6.4
cycler==0.10.0
joblib==0.14.1
kiwisolver==1.1.0
matplotlib==2.2.5
numpy==1.16.6
opencv-contrib-python==4.2.0.32
pandas==0.24.2
pickle-compat==2.1.0
pyparsing==2.4.7
python-dateutil==2.8.2
pytz==2021.3
PyYAML==5.4.1
scikit-learn==0.20.4
scipy==1.2.3
six==1.16.0
xlrd==1.0.0